// script.js

// Function to handle button click events
function selectOption(option) {
    // Check which option was clicked
    if (option === 'ClICK') {
        flashROSES(function() {
            document.getElementById('question').style.display = 'none'; // Hide the question
            displayROSES(); // Display the cat-heart.gif
        });
    }
}

// Function to display the cat.gif initially
function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE1.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE2.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE3.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE4.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE5.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE6.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };

}
function displayROSES() {
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat
    var ROSESimage = new Image();
    // Set the source (file path) for the cat image
    ROSESimage.src = 'ROSE7.gif'; // Assuming the cat image is named "cat.gif"
    // Set alternative text for the image (for accessibility)
    ROSESimage.alt = 'ROSE';
    // When the cat image is fully loaded, add it to the image container
    ROSESimage.onload = function() {
        imageContainer.appendChild(ROSESimage);
    };
}

// Function to display the cat-heart.gif
function displayROSES() {
    // Clear existing content in the image container
    document.getElementById('image-container').innerHTML = '';
    // Get the container where the image will be displayed
    var imageContainer = document.getElementById('image-container');
    // Create a new Image element for the cat-heart
    var ROSESImage = new Image();
    // Set the source (file path) for the cat-heart image
    ROSESImage.src = 'ROSE1.gif'; // Assuming the cat-heart image is named "cat-heart.gif"
    // Set alternative text for the image (for accessibility)
    ROSESImageImage.alt = 'Cat Heart';
    // When the cat-heart image is fully loaded, add it to the image container
    ROSESImageImage.onload = function() {
        imageContainer.appendChild(ROSESImage);
        // Hide the options container
        document.getElementById('options').style.display = 'none';
    };
}

// Display the cat.gif initially
displayROSES();